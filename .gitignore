# Allow all data, cache, and local vectors as requested
# But exclude massive library folders (node_modules, venv) to prevent GitHub push errors.

# Library folders (DO NOT PUSH THESE - use FIX_CLONE.bat instead)
node_modules/
venv/
env/
.env

# System/IDE
.vscode/
.idea/
.DS_Store

# Everything else is allowed (pycache, csv, npy, pkl, etc.)
# No other ignores.
